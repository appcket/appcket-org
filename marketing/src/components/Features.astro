---
import {
  BiLogoKubernetes,
  BiLogoPostgresql,
  BiLogoGraphql,
  BiLogoReact,
} from "react-icons/bi";
import { SiAstro } from "react-icons/si";
import { PiCertificate } from "react-icons/pi";
import { LuShieldCheck, LuMonitorCog } from "react-icons/lu";
---

<section class="w-full px-6 py-12 md:px-12 bg-white dark:bg-gray-900">
  <div class="max-w-6xl mx-auto">
    <h2 class="text-3xl font-arvo font-bold text-center mb-12">
      Everything you need to start working on your app
    </h2>
    <div class="grid md:grid-cols-3 gap-10">
      <div class="bg-blue-50 p-8 rounded-xl shadow flex flex-col items-center">
        <BiLogoKubernetes size={40} className="text-blue-900 mb-4" />
        <h3 class="font-bold text-xl text-blue-900 mb-2">Kubernetes</h3>
        <p class="text-gray-600 text-center">
          Local development with k8s (just like in prod) and an easy setup
          script to get you going fast.
        </p>
      </div>
      <div class="bg-blue-50 p-8 rounded-xl shadow flex flex-col items-center">
        <BiLogoPostgresql size={40} className="text-blue-900 mb-4" />
        <h3 class="font-bold text-xl text-blue-900 mb-2">Postgres Database</h3>
        <p class="text-gray-600 text-center">
          Data is stored in a Postgres database running locally with Docker
          Compose.
        </p>
      </div>
      <div class="bg-blue-50 p-8 rounded-xl shadow flex flex-col items-center">
        <BiLogoGraphql size={40} className="text-blue-900 mb-4" />
        <h3 class="font-bold text-xl text-blue-900 mb-2">GraphQL API</h3>
        <p class="text-gray-600 text-center">
          API based on NestJS and MikroORM ready with example queries and
          mutations.
        </p>
      </div>

      <div class="bg-blue-50 p-8 rounded-xl shadow flex flex-col items-center">
        <BiLogoReact size={40} className="text-blue-900 mb-4" />
        <h3 class="font-bold text-xl text-blue-900 mb-2">React App</h3>
        <p class="text-gray-600 text-center">
          A fully working Typescript React SPA bootstrapped with Vite, MUI and
          Tailwind that integrates with the API using React Query.
        </p>
      </div>
      <div class="bg-blue-50 p-8 rounded-xl shadow flex flex-col items-center">
        <SiAstro size={40} className="text-blue-900 mb-4" />
        <h3 class="font-bold text-xl text-blue-900 mb-2">Marketing Site</h3>
        <p class="text-gray-600 text-center">
          Blazing fast static site based on Astro for marketing your app.
        </p>
      </div>
      <div class="bg-blue-50 p-8 rounded-xl shadow flex flex-col items-center">
        <PiCertificate size={40} className="text-blue-900 mb-4" />
        <h3 class="font-bold text-xl text-blue-900 mb-2">
          https:// Everywhere
        </h3>
        <p class="text-gray-600 text-center">
          No fuss TLS certificates setup so you can work with https locally and
          in production.
        </p>
      </div>

      <div class="bg-blue-50 p-8 rounded-xl shadow flex flex-col items-center">
        <LuShieldCheck size={40} className="text-blue-900 mb-4" />
        <h3 class="font-bold text-xl text-blue-900 mb-2">Authn/Authz</h3>
        <p class="text-gray-600 text-center">
          User authentication and attribute-based authorization system that
          works on the server and client already setup with roles and
          permissions powered by a dedicated Keycloak instance.
        </p>
      </div>
      <div class="bg-blue-50 p-8 rounded-xl shadow flex flex-col items-center">
        <SiAstro size={40} className="text-blue-900 mb-4" />
        <h3 class="font-bold text-xl text-blue-900 mb-2">Example App</h3>
        <p class="text-gray-600 text-center">
          Appcket brings it all together with a full-featured team task app when
          you clone the <a
            href={`${import.meta.env.PUBLIC_GITHUB_URL}`}
            class="underline"
            target="_blank">appcket-org repo</a
          >. Get up and running quickly with how to create, read, update and
          delete teams projects and tasks.
        </p>
      </div>
    </div>
  </div>
</section>

<section class="w-full px-6 py-24 md:px-12 bg-white dark:bg-gray-900">
  <div class="max-w-6xl mx-auto">
    <div class="mb-24">
      <h2 class="text-3xl font-bold mb-6 font-arvo">
        Production ready codebase
      </h2>
      <div class="grid md:grid-cols-2 gap-12">
        <div>
          <p class="text-lg text-gray-600 dark:text-gray-300 mb-6">
            More marketing copy here. Ship faster with an opinionated starter
            kit that includes tests, and battle-tested patterns so you can focus
            on features instead of wiring services together.
          </p>
          <p class="text-lg text-gray-600 dark:text-gray-300">
            Modular services and clear interfaces make it easy to customize,
            replace, or scale parts of the stack as your product grows.
          </p>
        </div>
        <div class="bg-gray-900 rounded-lg p-6">
          <pre
            class="text-white font-mono text-sm"><code>npm run dev
# or
npx astro dev</code></pre>
        </div>
      </div>
    </div>

    <div>
      <h2 class="text-3xl font-bold mb-12 font-arvo">
        Flexible and extensible - More Marketing Copy
      </h2>
      <div class="grid md:grid-cols-3 gap-8">
        <div class="p-6 bg-gray-50 dark:bg-gray-800 rounded-lg">
          <svg
            class="w-12 h-12 text-indigo-600 mb-4"
            viewBox="0 0 24 24"
            fill="none"
            xmlns="http://www.w3.org/2000/svg"
          >
            <path
              d="M12 22C17.5228 22 22 17.5228 22 12C22 6.47715 17.5228 2 12 2C6.47715 2 2 6.47715 2 12C2 17.5228 6.47715 22 12 22Z"
              stroke="currentColor"
              stroke-width="2"></path>
            <path d="M12 8V16" stroke="currentColor" stroke-width="2"></path>
            <path d="M8 12H16" stroke="currentColor" stroke-width="2"></path>
          </svg>
          <h3 class="text-xl font-bold mb-2">Quick Setup</h3>
          <p class="text-gray-600 dark:text-gray-300">
            Bootstrap projects quickly using templates, examples, and a clear
            developer guide.
          </p>
        </div>
        <div class="p-6 bg-gray-50 dark:bg-gray-800 rounded-lg">
          <svg
            class="w-12 h-12 text-indigo-600 mb-4"
            viewBox="0 0 24 24"
            fill="none"
            xmlns="http://www.w3.org/2000/svg"
          >
            <path
              d="M20 7L12 3L4 7M20 7L12 11M20 7V17L12 21M12 11L4 7M12 11V21M4 7V17L12 21"
              stroke="currentColor"
              stroke-width="2"></path>
          </svg>
          <h3 class="text-xl font-bold mb-2">Flexible Integration</h3>
          <p class="text-gray-600 dark:text-gray-300">
            Pluggable adapters and hooks for databases, auth providers, and
            third-party services.
          </p>
        </div>
        <div class="p-6 bg-gray-50 dark:bg-gray-800 rounded-lg">
          <svg
            class="w-12 h-12 text-indigo-600 mb-4"
            viewBox="0 0 24 24"
            fill="none"
            xmlns="http://www.w3.org/2000/svg"
          >
            <path
              d="M9 12L11 14L15 10M21 12C21 16.9706 16.9706 21 12 21C7.02944 21 3 16.9706 3 12C3 7.02944 7.02944 3 12 3C16.9706 3 21 7.02944 21 12Z"
              stroke="currentColor"
              stroke-width="2"></path>
          </svg>
          <h3 class="text-xl font-bold mb-2">Secure by Default</h3>
          <p class="text-gray-600 dark:text-gray-300">
            Secure defaults, observability, and tooling that help you ship
            safely in production.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>

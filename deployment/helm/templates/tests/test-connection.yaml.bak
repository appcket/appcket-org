apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "appcket.fullname" . }}-test-connection"
  labels:
    {{- include "appcket.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "appcket.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
